export class Constants {
    // Acceleration due to gravity on Earth (in m/s^2)
    static readonly g = 9.81;

    // Speed of light in vacuum (in m/s)
    static readonly c = 299792458;

    // Planck's constant (in Joule seconds)
    static readonly h = 6.62607015e-34;

    // Elementary charge (in Coulombs)
    static readonly e = 1.602176634e-19;

    // Avogadro's number (in mol^-1)
    static readonly N_A = 6.02214076e23;

    // Boltzmann constant (in Joules per Kelvin)
    static readonly k = 1.380649e-23;

    // Universal gas constant (in Joules per mole-Kelvin)
    static readonly R = 8.314462618;

    // Electron mass (in kilograms)
    static readonly m_e = 9.10938356e-31;

    // Proton mass (in kilograms)
    static readonly m_p = 1.67262192369e-27;

    // Neutron mass (in kilograms)
    static readonly m_n = 1.67492749804e-27;

    // Earth's radius (in meters)
    static readonly R_earth = 6371000;

    // Universal gravitational constant (in m^3 kg^-1 s^-2)
    static readonly G = 6.67430e-11;

    // Standard atmospheric pressure at sea level (in Pascal)
    static readonly P_atm = 101325;

    // Coulomb's constant (in N m^2 C^-2)
    static readonly k_e = 8.9875517873681764e9;

    // Pi (π)
    static readonly pi = Math.PI;

    // Euler's number (e)
    static readonly eulersNumber = Math.E;

    // Universal gravitational acceleration (g) on Earth's surface (in m/s^2)
    static readonly g_earth = 9.81;
}
